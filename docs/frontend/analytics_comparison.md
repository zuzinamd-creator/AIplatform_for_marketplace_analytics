# Analytics comparison UX

Comparison mode supports:

- current period vs previous period (auto-computed)
- custom period A vs period B

Displayed:

- absolute delta
- % change (when baseline is non-zero)
- trust signals (freshness, integrity warnings, completeness)

