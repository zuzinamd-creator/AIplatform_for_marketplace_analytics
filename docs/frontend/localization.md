# Russian localization

Policy:

- seller-facing pages must be **Russian-only**
- internal ops pages can remain English if clearly separated

Updated in this phase:

- dashboard page (`/app/dashboard`)
- onboarding page (`/app/onboarding`)
- main navigation labels (`AppShell`)

