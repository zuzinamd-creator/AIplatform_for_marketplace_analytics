# Inventory economics (deterministic) — placeholder

This phase will expose inventory economics from governed snapshots (`warehouse_stock_snapshots`) and costs (`cost_history`):

- frozen capital (stock × unit cost)
- slow movers
- turnover
- dead stock
- storage burden proxies

Implementation is additive and read-only; no ledger mutation.

