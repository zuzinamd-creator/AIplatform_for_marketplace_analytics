-- Optional manual schema reference (app also runs create_all on startup)
-- Supabase PostgreSQL

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Enums and tables are created by SQLAlchemy metadata.
-- Use this file for documentation or manual DBA review.
