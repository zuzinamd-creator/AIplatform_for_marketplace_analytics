# No Docker: API via uvicorn on :8000, UI via Vite on :5173
VITE_API_BASE_URL=http://localhost:8000
VITE_API_PREFIX=/api/v1

# With Docker + nginx use instead:
# VITE_API_BASE_URL=http://localhost:8080
