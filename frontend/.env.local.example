# Local dev — Vite proxies API calls to nginx or uvicorn
VITE_API_BASE_URL=http://localhost:8080
VITE_API_PREFIX=/api/v1

# If using Vite dev server only (npm run dev), default port is 5173:
# Open http://localhost:5173
