/** Frontend feature flags for phased rollout (Phase 9.6B+). */
export const FEATURE_FLAGS = {
  /** When true, AppShell mounts the global CostTrustBanner with live integrity data. */
  costTrustBannerGlobal: false,
} as const;
